This is an Angular application with Typescript.

Mission: handy tool for scouts to use for delivering songs texts in offline mode.

Key features:

* It uses built-in PWA features to deliver content offline. Content (songs) is defined as na asset cached by the PWA, no backend required.
* No mainenance cost: app has no backend and it is hosted on GitHub pages.

